---
const authorData = {
  author: [
    {
      left: [
        {
          tag1: "大学小牛马",
          tag2: "摆烂普通人",
          tag3: "一直在学习",
          tag4: "不会创新只会CV"
        }
      ],
      right: [
        {
          tag1: "博客写作者",
          tag2: "小说爱好者",
          tag3: "资源分享者",
          tag4: "很喜欢Astro"
        }
      ]
    }
  ]
};
---

<div class="author-content-item author-main">
  <div id="author-main">
    {authorData.author.map((author, authorIndex) => (
      <>
        <div class="author-tag-left">
          {author.left.map((left, leftIndex) => (
            <>
              <span class="author-tag">{left.tag1}</span>
              <span class="author-tag">{left.tag2}</span>
              <span class="author-tag">{left.tag3}</span>
              <span class="author-tag">{left.tag4}</span>
            </>
          ))}
        </div>
        <div class="mainports" style="margin: 0 20px;">
          <div class="author-box" style="z-index:0">
            <div class="author-img">
              <img class="no-lightbox" src="/avatar.webp" style="width: 180px; border-radius: 50%;" alt="avatar" />
            </div>
          </div>
        </div>
        <div class="author-tag-right">
          {author.right.map((right, rightIndex) => (
            <>
              <span class="author-tag">{right.tag1}</span>
              <span class="author-tag">{right.tag2}</span>
              <span class="author-tag">{right.tag3}</span>
              <span class="author-tag">{right.tag4}</span>
            </>
          ))}
        </div>
      </>
    ))}
  </div>
</div>

<style scoped>
/* author */
#author-main {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 0 16px 0;
  user-select: none;
}

.author-box {
  position: relative;
  width: 180px;
  height: 180px;
  background: transparent;
  border-radius: 50%;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}

.author-img {
  margin: 0 30px;
  border-radius: 50%;
  width: 180px;
  height: 180px;
  position: relative;
  background: transparent;
  user-select: none;
  transition: .3s;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 移除重复的floating动画定义，使用全局定义 */
</style>